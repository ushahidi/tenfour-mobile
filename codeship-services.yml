builder:
  build: .
  volumes:
    - "./tmp/pwa:/var/app/www"

awscli:
  image: ushahidi/engineering-deployer:latest
  volumes:
    - .:/vols/src
  encrypted_env_file: deployment.env.encrypted
  cached: true
